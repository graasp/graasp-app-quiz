import QuestionDifficulty from './QuestionDifficulty';

const AnalyticsMenu = () => {
  // TODO add menu later once previous PR is merged, so that I can beneficial from the refactoring
  return <QuestionDifficulty />;
};

export default AnalyticsMenu;
