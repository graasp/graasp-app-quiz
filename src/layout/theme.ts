import { createTheme } from '@mui/material/styles';

const graaspTheme = createTheme({
  palette: {
    primary: {
      main: '#555BD9',
    },
    background: {
      default: 'transparent',
    },
  },
});

export default graaspTheme;
